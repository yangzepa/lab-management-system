# Lab Management System - Environment Variables
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요
# cp .env.example .env

# ===================================
# MySQL Database Configuration
# ===================================
# MySQL root 계정 비밀번호 (강력한 비밀번호 사용 권장)
MYSQL_ROOT_PASSWORD=your_secure_password_here_change_this

# ===================================
# JWT Configuration
# ===================================
# JWT 토큰 서명에 사용되는 비밀키
# 최소 32자 이상의 랜덤 문자열을 사용하세요
# 생성 방법: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_at_least_32_characters_long_change_this

# JWT 토큰 만료 시간 (밀리초)
# 86400000 = 24시간
JWT_EXPIRATION=86400000

# ===================================
# Domain Configuration (Production)
# ===================================
# 프로덕션 도메인 (HTTPS용)
DOMAIN=ec.sch.ac.kr

# ===================================
# Spring Profile (Optional)
# ===================================
# 활성화할 Spring Profile
# dev: 개발 환경
# prod: 프로덕션 환경
SPRING_PROFILES_ACTIVE=prod

# ===================================
# 보안 참고사항
# ===================================
# 1. 이 파일(.env)은 절대 Git에 커밋하지 마세요!
# 2. .gitignore에 .env가 포함되어 있는지 확인하세요
# 3. 프로덕션 환경에서는 강력한 비밀번호를 사용하세요:
#    - 최소 16자 이상
#    - 대소문자, 숫자, 특수문자 혼합
#    - 추측하기 어려운 랜덤 문자열
# 4. .env 파일의 권한을 제한하세요: chmod 600 .env
# 5. 정기적으로 비밀번호와 JWT Secret을 변경하세요

# ===================================
# 비밀번호 생성 방법
# ===================================
# MySQL 비밀번호:
# openssl rand -base64 24
#
# JWT Secret:
# openssl rand -base64 32
